package org.example.ftblr.Entity;

public enum Visibility {
    PUBLIC,            // Visible par tous
    PRIVATE,           // Visible uniquement par invitation
    FRIENDS_ONLY      // Visible uniquement par les amis
}
